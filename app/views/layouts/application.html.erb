<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Choco Explorer" %></title>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <!-- Favicon -->
    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <!-- Google Fonts: Nunito -->
    <link href="https://fonts.googleapis.com/css2?family=Nunito:wght@400;600;800&display=swap" rel="stylesheet">

    <!-- Bootstrap 5 CSS (CDN) -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-Gtv8JvpA4Q2rRU28XKQ6jTQ9x7mTq9Lj3YJW0VXU3evFUKLgUCSmUyMvYjHqobJX" crossorigin="anonymous">

    <!-- Stylesheets -->
    <%= stylesheet_link_tag "application", media: "all", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="main-body" style="margin: 0; font-family: 'Nunito', sans-serif;">
    <nav style="background-color: #5a4a8c; padding: 1rem 2rem; display: flex; align-items: center; justify-content: space-between; flex-wrap: wrap;">
      
      <!-- Left: Site title -->
      <div style="color: white; font-weight: 800; font-size: 1.5rem;">
        Choco Explorer
      </div>

      <!-- Middle: Navigation links -->
      <div style="margin-top: 0.5rem;">
        <%= link_to "Chocolates", products_path, style: "color: #fff2e6; margin-right: 1rem;" %>
        <%= link_to "Brands", brands_path, style: "color: #fff2e6; margin-right: 1rem;" %>
        <%= link_to "Categories", categories_path, style: "color: #fff2e6; margin-right: 1rem;" %>
        <%= link_to "Countries", countries_path, style: "color: #fff2e6; margin-right: 1rem;" %>
      </div>

      <!-- Right: Search form -->
      <div style="margin-top: 0.5rem;">
        <form action="<%= products_path %>" method="get" style="display: flex; gap: 0.5rem; align-items: center;">
          <input type="text" name="query" value="<%= params[:query] %>" placeholder="Search Chocolate" style="padding: 0.4rem;" />
          <button type="submit" style="padding: 0.4rem 0.75rem; background-color: #fff2e6; border: none; cursor: pointer;">
            Search
          </button>
        </form>
      </div>
    </nav>

    <main style="padding: 2rem;">
      <%= yield %>
    </main>
    <!-- Bootstrap 5 JS (optional but useful for dropdowns, modals, etc.) -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js" integrity="sha384-ENjdO4Dr2bkBIFxQpeoFf1A0Hqyt6bUuh0pIJ5j2kFvGS1V8AKPPYV+jGfl49kZF" crossorigin="anonymous"></script>
  </body>
</html>
